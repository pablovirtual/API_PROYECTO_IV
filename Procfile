web: php artisan serve --host=0.0.0.0 --port=$PORT
```
## 2. Asegurar que tienes un archivo .env.example en tu repositorio

Verifica que tu archivo `.env.example` existe y contiene la configuración básica necesaria.

## 3. Mantener el Procfile para Railway
